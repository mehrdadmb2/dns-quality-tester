# dns-quality-tester
dns-quality-tester-pro — production-ready DNS benchmarking tool. Run configurable multi-protocol tests (UDP with TCP fallback and DoH), multiple qtypes (A/AAAA/MX/NS), detect DNSSEC AD flag, measure latency and disagreement vs majority answers. Outputs raw CSV and aggregated JSON for automation, nightly checks, and research.
